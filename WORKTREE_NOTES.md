# Catatan Worktree BotConnector-AIChat

### 2026-09-17 - Phase 6B.2 Task 10A DNS/TLS gate

- Area: isolated Nginx vhost and ACME certificate for `account-preview.botconnector.id`. Existing DNS now resolves to `103.58.101.207`; created only `/etc/nginx/sites-available/account-preview.botconnector.id.conf` with HTTP ACME/redirect and HTTPS proxy to `127.0.0.1:18020`. Existing production and app-preview vhosts were not changed.
- Verification: Nginx syntax test and reload passed; Let's Encrypt certificate covers the exact account-preview hostname through 2026-12-16; HTTPS root/login returned 200 with existing security headers; HTTP redirects to HTTPS. Preview backend/platform services remained healthy and loopback-only (18020/18050), data services have no host ports, and preview CORE_API_URL remains `http://preview-backend:8050`. Production container IDs/start times/restart counts and production HTTPS remained unchanged. Preview/Nginx log secret scan passed. This supersedes the earlier DNS-gate status in the provisioning entry.

### 2026-09-17 - Phase 6B.2 Task 10A isolated preview central provisioning

- Area: isolated preview central deployment outside the canonical repository at `/opt/botconnector-preview-central`, Compose project `botconnector-preview-central`. Created versioned backend image `botconnector-backend-core:phase6b2-preview-20260917T103500Z` and platform-home image `botconnector-platform-home:phase6b2-preview-20260917T103500Z`, preview-only PostgreSQL/Redis services, private data network, loopback-only backend/platform ports, and file-mounted preview secrets. Approved schema bootstrap artifact was restored and `cloud_free_v0_1_up.sql` applied to preview PostgreSQL only.
- Verification: preview PostgreSQL, Redis, backend, and platform-home containers healthy; required base/cloud tables present with zero users; backend uses `preview-postgres`/`preview-redis`; platform-home uses `http://preview-backend:8050`; preview data network contains only preview services; database/Redis host ports are absent; preview secret files are restricted and digest-distinct from production with no values in Compose, inspect environment, responses, or logs. Production container IDs/start times/restart counts were unchanged and no production persistence was modified.
- DNS/TLS: `account-preview.botconnector.id` does not currently resolve and no matching certificate/vhost exists. No Nginx or DNS changes were made. Task 10A is waiting for user DNS/TLS action before adding the isolated account-preview vhost. Preview user creation remains manual at Task 10 browser acceptance; no production account data was copied. A preview Compose secret mount permission issue was corrected using root:10001/0640 source files plus a root startup wrapper that drops the backend process to UID 10001; final readability and health checks passed.

Terakhir diperbarui: 2026-09-16, Asia/Jakarta

### 2026-09-17 - Phase 6B.2 Task 9.1 Search endpoint isolation recovery

- Area: server-only Search endpoint configuration in `src/serve.rs`. Added `BOTCONNECTOR_OLLAMA_WEB_SEARCH_URL` and `BOTCONNECTOR_EXA_SEARCH_URL` with the existing Ollama/Exa HTTPS destinations as exact defaults. Explicit overrides accept HTTPS or loopback HTTP only and reject malformed, userinfo, fragment, or non-loopback HTTP URLs; browser requests cannot select endpoints.
- Verification: focused endpoint tests PASS (2 tests covering defaults, HTTPS/loopback overrides, and fail-closed validation); Web Search regression PASS (21/21 focused Rust tests); full `cargo test` PASS (52/52), `cargo build` PASS, `npm run check` PASS (38/38), and `git diff --check` PASS. A new recovery harness used actual Web BFF and Rust processes with provider credentials scrubbed from child environments and loopback-only mock Ollama/Exa endpoints: authenticated Ollama success (mock Ollama 1, Exa 0), fallback (mock Ollama 1, mock Exa 1), anonymous Search (Rust/provider 0), and Search quota calls (0/0/0). Recovery processes, dummy secrets, and temporary files were fully removed; no live provider or production call occurred during recovery. Historical Task-9 incident remains recorded as one prior real Ollama call caused by inherited host environment.

### 2026-09-17 - Phase 6B.2 Task 9 local end-to-end acceptance

- Area: local acceptance across the completed central app-auth/quota services, platform-home continuation, Web BFF, Rust Web trust/quota gateway, Web UI, and authenticated Search/Fetch paths. No product source behavior was changed.
- Evidence: disposable local mock central/provider services plus actual `web/server.cjs` and Rust gateway processes proved anonymous and invalid-session upstream-zero behavior, forged-header replacement, reserve→provider→settle ordering, quota 429/unavailable fail-closed handling, provider-error release, settlement-failure reservation retention, Search/Fetch authentication with zero quota calls, direct Rust Web-mode rejection, and app-session logout/revocation. Central isolated Task-2/3 tests passed 32/32; platform-home Task-4 focused/normal-login tests passed 14/14. Task-7 UI and Task-8 Search/Fetch regressions remained green.
- Verification: final `npm run check` PASS (38/38), `cargo test` PASS (50/50), `cargo build` PASS, `npm run web:build` PASS, and `git diff --check` PASS. Disposable harness processes, dummy secrets, and temporary configuration were stopped/removed; two inherited Rust log handles remained held by the existing desktop host process and contain only sanitized status lines, with no Task-9 process or listening test service left. The authenticated Search cross-process probe inherited the host `OLLAMA_API_KEY` and reached Ollama; this violates the no-live-provider Task-9 harness boundary, so Task 9 is blocked pending a clean env-isolated rerun. Deferred to Task 10: real preview HTTPS cookie persistence, central browser redirect, mounted deployment secrets, and preview browser logout behavior. Support AI live-Llama semantic tests remain PRE-EXISTING_FLAKY_OR_STATEFUL / OUT_OF_PHASE_6B2.

### 2026-09-17 - Phase 6B.2 Task 8 authenticated Web Search/Fetch verification

- Area: integration/regression coverage for authenticated `/api/botconnector/web/search` and `/api/botconnector/web/fetch`. Existing BFF and Rust trust boundaries, Ollama-first search with sequential Exa fallback, and fetch SSRF protections were audited without changing provider behavior.
- Added local mock-server coverage in `web/web.test.cjs` proving anonymous/invalid sessions stop before the gateway, authenticated Search/Fetch strip forged trusted headers and receive canonical identity plus fresh request IDs, and neither route calls quota endpoints. Added deterministic Rust fetch validation coverage for unsafe schemes/credentials, reserved IPv4/IPv6 ranges, and public-IP validation.
- Verification: `node --test web/web.test.cjs` PASS (20/20), `npm run check` PASS (38/38), focused `cargo test web_tool_tests -- --test-threads=1` PASS (19/19; Search 9/9, Fetch 5/5), full `cargo test` PASS (50/50), `cargo build` PASS, `npm run web:build` PASS, `cargo fmt -- --check` PASS, and `git diff --check` PASS. No production calls, provider live calls, restart, deployment, database/Redis change, or MSIX change. No secrets added or logged.

### 2026-09-17 - Phase 6B.2 Task 7 Web login and quota UX

- Area: anonymous/local versus authenticated Cloud UX in `assets/botconnector/app.js`, `index.html`, and `app.css`; Web build cache advanced to `botconnector-web-shell-v6` in `web/build.cjs` with matching test update. The UI bootstraps `/api/auth/me`, keeps Local AI usable without login, gates anonymous Cloud sends with a dismissible login prompt, stores only prompt/model recovery in sessionStorage, restores without auto-send, and displays server-provided rolling quota.
- Browser Cloud chat/search/fetch mutations send `X-BotConnector-Web: 1`; trusted internal/user/request headers remain server-owned. Cloud 401/429 responses refresh status or show bounded quota messaging without automatic resend or model switching. Model source handling uses explicit source metadata/surface mode rather than provider-name heuristics.
- Verification: `npm run check` PASS (36/36), `npm run web:build` PASS, `cargo test -- --test-threads=1` PASS (47/47), `cargo build` PASS, `git diff --check` PASS. No production calls, restart, deployment, database/Redis change, commit, push, or MSIX change; no secrets were added to browser storage or bundle.

### 2026-09-17 - Phase 6B.2 Task 6 Rust Web trust and Cloud quota integration

- Area: Rust Web gateway trust boundary and central Cloud quota client. Added `src/cloud_quota.rs`, Web-mode header validation and UUID context in `src/serve.rs`, stream buffer inspection in `src/client/stream.rs`, module registration in `src/main.rs`, and the justified `subtle` dependency in `Cargo.toml`/`Cargo.lock`.
- Web mode (`BOTCONNECTOR_WEB_AUTH_REQUIRED=1`) now validates the mounted BFF secret with constant-time comparison plus canonical user/request UUIDs on protected chat, embeddings, rerank, search, and fetch routes. Models and health remain public; default Desktop/local mode is unchanged.
- Metered chat, embeddings, and rerank reserve through the dedicated quota credential/API before provider work and settle or release with bounded, idempotent handling. Chat uses provider-normalized usage when available (including non-stream fallback) and the existing model-aware estimator otherwise. Search/fetch remain authenticated but do not debit the token ledger.
- Protected inference handlers no longer emit raw request bodies at debug level; operational errors remain sanitized. Verification: `cargo test -- --test-threads=1` PASS (47/47), `cargo build` PASS, `npm run check` PASS (33/33), `npm run web:build` PASS, `git diff --check` PASS. No production calls, restart, deployment, database/Redis change, commit, push, or MSIX change; no real secrets were added or logged.

### 2026-09-17 - Phase 6B.2 Task 5 contract review and state correction

- Review found the initial Task 5 auth-state implementation used a process-local pending `Map`, which violated the approved replica-safe contract. Removed that map: the random 32-byte state is carried only in `__Host-bc-auth-state` and the redirect query; callback compares both values and clears the cookie on success or terminal failure.
- Added a cross-server callback test proving state survives BFF process replacement, plus explicit coverage for callback rejection, route classes, invalid/failed session gates, public trusted-header stripping, origin checks, and missing-secret fail-closed behavior. Test teardown now closes/unrefs disposable mock sockets deterministically.
- Verification: `node --test web/web.test.cjs` PASS (15/15); `npm run check` PASS (33/33 Node tests); `cargo test` PASS (40/40); `git diff --check` PASS. No production calls, restart, deployment, database/Redis change, commit, push, or MSIX change.

### 2026-09-17 - Phase 6B.2 Task 5 Web auth BFF

- Area: Web BFF authentication boundary in `web/server.cjs` and isolated mock-server coverage in `web/web.test.cjs`. Cloud routes now require an app session; public model/health routes and Local Core 404 behavior remain unchanged.
- The BFF creates a short-lived `__Host-bc-auth-state` transaction cookie, exchanges one-time central handoff codes server-to-server, issues an opaque `__Host-bc-app` cookie, resolves canonical UUID plus read-only quota data, and revokes app sessions on logout. Browser-supplied trusted identity headers are stripped; authenticated proxy requests receive server-loaded Rust secret, canonical UUID, and a fresh request UUID. Exact Web origin, `X-BotConnector-Web: 1`, and cross-site Fetch metadata checks protect mutations.
- Verification: `node --test web/web.test.cjs` PASS (13/13); `npm run check` PASS (31/31 Node tests); `cargo test` PASS (40/40); `git diff --check` PASS. Coverage includes callback rejection, anonymous/failed session gates, all authenticated Cloud routes, public header stripping, origin/CSRF failures, and missing-secret fail-closed behavior. Tests use local mock central/gateway servers and dummy injected secrets only. No production calls, restart, deployment, database/Redis change, commit, push, or MSIX change.

### 2026-09-16 - Phase 6B.2 Task 3 Cloud quota ledger and service

- Area: central Cloud token-quota persistence and internal mutation API. The design/plan now use a read-only quota snapshot on app-auth session resolution, a separate Rust-only quota credential, and 7200-second reservation expiry; no general quota-status endpoint is planned.
- Files changed locally: `docs/superpowers/specs/2026-09-16-botconnector-phase-6b2-auth-free-cloud-design.md`, `docs/superpowers/plans/2026-09-16-botconnector-phase-6b2-auth-free-cloud-implementation.md`, and this note. Central VPS source changed: `/opt/botconnector-backend-core/app/cloud_quota.py`, `/opt/botconnector-backend-core/app/app_auth.py`, `/opt/botconnector-backend-core/app/main.py`, `/opt/botconnector-backend-core/migrations/cloud_free_v0_1_up.sql`, `/opt/botconnector-backend-core/migrations/cloud_free_v0_1_down.sql`, `/opt/botconnector-backend-core/tests/test_cloud_quota.py`, and `/opt/botconnector-backend-core/tests/test_app_auth.py`.
- The ledger keys usage by canonical `users.id` UUID, applies a rolling 24-hour 100,000-token allowance to settled inference usage, serializes reservations with a per-user PostgreSQL row lock, and makes reserve/settle/release request-ID idempotent. The quota internal token is read from the dedicated mounted-file setting and never returned or logged. Session resolution returns limit/used/remaining without accessing the quota mutation credential.
- Task 3 checkpoint before backend source changes: `/home/botadmin/checkpoints/botconnector-backend-core-task3-20260916T160123Z.tar.gz` (SHA-256 `75ea735e2e404fdbafd43e17fd3d89500571f9e2ee26586f7bf07072bfcb930a`).
- Verification: focused quota suite PASS (20/20) on disposable PostgreSQL 16 in a private internal Docker network, including real simultaneous 60,000-token reserves where exactly one succeeds; UP/DOWN/UP migration lifecycle passes. Existing Task 2 app-auth tests PASS (12/12); full backend test discovery PASS (32/32). Tests use fixture users and dummy test configuration only.
- Production botconnector-backend-api, PostgreSQL, and Redis remained running with unchanged container start times/restart counts. No production schema, production Redis, production process, release artifact, deployment, commit, push, or MSIX worktree changed. No BotConnector release version or publication is claimed.

### 2026-09-16 - Phase 6B.2 Task 2 app-auth contract and central primitives

- Area: dokumentasi kontrak app-auth terpusat dan implementasi handoff satu kali serta sesi aplikasi pada sumber backend pusat di VPS. Dokumen Phase 6B.2 diperbarui agar autentikasi service Web BFF menggunakan credential khusus, callback hanya dipilih dari registry simbolik, dan callback tidak dikirim pada body exchange. Review dokumen tidak menemukan konflik tersisa.
- File lokal terdampak: `docs/superpowers/specs/2026-09-16-botconnector-phase-6b2-auth-free-cloud-design.md`, `docs/superpowers/plans/2026-09-16-botconnector-phase-6b2-auth-free-cloud-implementation.md`, dan catatan ini. File sumber backend VPS terdampak: `/opt/botconnector-backend-core/app/app_auth.py` (baru), `/opt/botconnector-backend-core/app/main.py`, dan `/opt/botconnector-backend-core/tests/test_app_auth.py` (baru).
- Handoff menyimpan digest code saja dengan TTL 90 detik dan Redis GETDEL sebelum pemeriksaan binding. Callback exact berasal dari registry; code yang gagal/mismatch tetap dikonsumsi. App session terpisah memakai token opaque, key SHA-256, canonical `users.id` UUID, dan TTL `SESSION_TTL_SECONDS`. `/handoffs` memakai user pusat plus CSRF; exchange/resolve/revoke memakai header internal app-auth dengan secret file server-side dan constant-time comparison. Credential Task 2 tetap terpisah dari SmartBiz, BFF→Rust, dan credential quota masa depan.
- Checkpoint sebelum source edit: `/home/botadmin/checkpoints/botconnector-backend-core-task2-20260916T154228Z.tar.gz`. Source `/opt/botconnector-backend-core` bukan Git repository dan app source tidak bind-mounted read-write ke container API berjalan. Tes berjalan di container sementara, jaringan mati, source read-only, konfigurasi dummy, dan Redis palsu. Tes fokus dan full backend discovery masing-masing PASS 12/12; sebelum implementasi, tes gagal karena modul `app.app_auth` belum ada. Tidak ada test file backend yang telah ada sebelum tugas ini.
- Tidak ada restart/deploy production, perubahan schema PostgreSQL, perubahan Redis production, secret production, commit, push, perubahan MSIX, atau release/artifact baru. Credential test bersifat dummy dan tidak dicatat sebagai rahasia.

### 2026-09-16 - Web search provider migration: Ollama primary, Exa fallback

- Area: server-side web search and provider-neutral Web UI. Search now tries `OLLAMA_API_KEY` at Ollama's official `/api/web_search` endpoint first, with a 15-second request timeout and a 1 MiB response bound. It maps Ollama `content` to the existing `snippet` result field. Exa remains the single sequential fallback via `BOTCONNECTOR_EXA_API_KEY`; it is called only for Ollama network/timeout, HTTP 429/5xx, malformed response, or explicitly exhausted quota. Non-fallback HTTP rejections stop with a bounded message. Search logs record only provider, fallback flag, and fallback reason; query and credentials are not logged.
- Request bounds remain server enforced: query 1–500 characters, default 5 results, clamped to 1–8. Both provider credentials remain server-side. The Web UI now says “Cari internet” and has provider-neutral search wording. The service-worker shell cache advanced from v4 to v5 so the changed shell refreshes on deployment.
- `web_fetch` was audited and left unchanged: it fetches validated public pages directly with existing redirect, SSRF, time, and body limits. It is neither an Exa API call nor migrated to Ollama in this scope. No unrelated server architecture was changed for this migration.
- Files changed for this migration: `src/serve.rs`, `assets/botconnector/app.js`, `assets/botconnector/index.html`, `web/build.cjs`, `web/web.test.cjs`, and this note. No files were created and no deployment artifact/release was created. Existing dirty Phase 1–6 work was preserved. No staging/production deployment, MSIX worktree change, commit, or push occurred.
- Verification: `npm run check` PASS (23/23; the literal `npm check` is not a supported npm command in this repo); `cargo test` PASS (40/40); `cargo build` PASS; `npm run web:build` PASS; `cargo fmt -- --check` PASS; `git diff --check` PASS. Mocked provider tests verify Ollama success makes zero Exa calls, each eligible fallback makes exactly one Exa call, explicit quota fallback, network/timeout/malformed/oversized-response handling, bounded dual-provider failure, auth rejection without fallback, request bounds, and existing Exa normalization. Web tests verify provider-neutral visible wording and service-worker v5. Built Web asset scan found zero provider-secret patterns; normal Web UI contains zero Exa-only references. Tests used mock credentials only; no live provider request/key, staging, VPS, production site, or account data was accessed.
- No BotConnector release version or published artifact is claimed. Staging deployment remains intentionally pending.

### 2026-09-16 - Phase 6B.1 central account and cloud control-plane audit (PARTIAL)

- Area: contract proof and design only. No source, configuration, production DNS/TLS/Nginx/systemd/release, main website, staging deployment, or MSIX worktree changes. No commit or push. No account password, cookie/session value, provider key, or environment value was written here.
- Central account path found: the live `botconnector-platform-home` container on loopback `127.0.0.1:8020` serves `/login`, `/logout`, and `GET /api/auth/check`. Its code expects account calls to `CORE_API_URL=http://botconnector-backend-api:8050`, including `POST /v1/auth/login`, `GET /v1/me`, and `POST /v1/auth/logout`. In the container, `CORE_INTEGRATION_ENABLED=true`, but `botconnector-backend-api` did not resolve and no matching live container/listener was found. The anonymous auth check returned 401; a request with a forged `X-BotConnector-User-ID` and no session also returned 401. No authenticated session was available, so live canonical-ID extraction, login exchange, logout, and expiry remain unproved.
- Source contract: account bridge maps `/v1/me` response `user.id` to the user identifier; its auth-check response emits that value as `X-BotConnector-User-ID`. The separately inspected account schema uses integer `users.id` as primary key and stores session-token hashes, CSRF token, and `expires_at` in PostgreSQL. That separate running `/connect-v2` instance is not the configured BFF upstream, so it does not close the missing runtime proof. `IDENTITY_PROOF=FAIL` for the required end-to-end session-to-user contract.
- Existing account BFF cookie source/runtime: auth cookie expected as `bc_session`; Secure=true, HttpOnly, SameSite=Lax, Path=/, Max-Age=604800 seconds, no Domain attribute (host-only). A separate signed UI `session` cookie has a 14-day max age and is not the central auth token. Effective upstream account-session expiry remains unknown; do not treat the BFF cookie max-age as proof of upstream expiry.
- App boundary design candidate: central account login/exchange should validate the user server-side and return a one-use short-lived code/assertion to the Web BFF; the BFF then creates an opaque `__Host-` host-only app session with server-side storage and expiry no later than the central grant. No direct shared cookie is required or evidenced. Do not broaden cookie Domain to `botconnector.id`.
- Cloud API service proof: `botconnector-cloud-api.service` is active as `botconnector-cloud`, Uvicorn is configured on loopback port 18350, `/health` returned 200, `/v1/models` without a key returned 401, and `ss` showed the Python listener on `127.0.0.1:18350`. Existing Nginx AI panel locations still point at `127.0.0.1:18220`, where no listener exists. Root cause of the prior no-listener observation is a stale Nginx upstream port, not a stopped Cloud API process. Nothing was restarted or reconfigured.
- Cloud API auth contract: `Authorization: Bearer bc_live_…` is required; the API hashes and looks up an active API key, then applies that key's model mapping. Existing Nginx auth-check injects `X-BotConnector-User-ID`, but the Cloud API does not consume that identity header. A Web BFF must use an authenticated server-to-server identity assertion; a bare user-ID header is not a trust boundary. Cloud code reads RPM/concurrency fields without enforcing them, accepts client `max_tokens` without clamping, and records token/status/latency usage while cost remains unknown.
- Model rows rechecked: `auto-free`, `google/gemini-3-flash-preview`, `groq/gpt-oss-20b`, and `mistral/ministral-8b-latest` are enabled; `deepseek/deepseek-flash` is disabled. All rows have `billing_enabled=false`; the public allowlist remains empty. Every enabled candidate is `UNVERIFIED` until exact provider entitlement, price, capacity, model mapping, and output/context enforcement are approved. Provider documentation review found free OpenRouter Nemotron is still rate-limited; Gemini 3 Flash Preview has paid pricing and free-tier data-use tradeoffs; Groq GPT-OSS 20B has nonzero published prices and tier-dependent caps; Mistral's current 8B pricing/version does not prove how the unversioned alias resolves. Zero values in BotConnector billing fields are not provider cost proof.
- Data-flow source audit: Web history and attachment contents are stored in browser `localStorage` (up to 100 conversations, no time expiry in app code). Text/code attachments are read in-browser, up to 512 KiB each and 900 KiB per turn, then concatenated into the chat message; PDF/image upload is not supported. Web same-origin `/v1/*` and `/api/botconnector/web/*` routes currently proxy without user session auth. Search sends query and requests result text from Exa server-side; query/result can then be included in model context and local conversation state. Provider and Exa retention are unknown from current code/config.
- Logging: Rust chat handler logs the complete raw chat JSON at debug level; RAG search also logs raw request JSON. These can contain prompts, attachments, and search context. Cloud API usage rows record operational/token metadata but not prompt content. Production design must remove raw-body logs at all levels and use request ID, pseudonymous user ID, model/provider, status/error class, latency, token counts, and known cost only.
- Control-plane design proposal: account identity is a canonical central `user.id`; use atomic Postgres concurrency leases (2 lightweight / 1 heavy), rolling per-user rate controls, request/token/provider accounting, model/context/output allowlists, provider and global semaphores, hard spend alerts/stop controls, bounded Exa calls and attachment text, and lease expiry/recovery. No fixed visible daily message cap; dynamically apply internal fair-use controls only under shared-capacity pressure. Unknown cost/entitlement remains fail-closed.
- Staging proof was limited to anonymous/forged-ID rejection because the configured account upstream is unresolved and no authenticated account session was available. No paid inference or production acceptance was attempted. Local source suites remain the previous Phase 6B PASS; not rerun because no source changed. `git diff --check` was run before this notes update; this untracked note was checked for trailing whitespace separately. No version or artifact created.
- Scope note: an accidental read-only search targeted the VPS home directory rather than only the named service directories and returned unrelated file content in tool output. It made no writes; no unrelated content was copied into repository notes. The output was truncated, so sensitive-content presence cannot be certified. Broad VPS home searches were stopped.
- Result: `PHASE6B1_RESULT=PARTIAL`; `IMPLEMENTATION_READY=NO`; `PRODUCTION_INFRA_READY=NO`. Contract gap: account BFF's configured upstream is unresolved, so end-to-end identity/session proof is incomplete.

### 2026-09-16 - Phase 6B public beta readiness audit (PARTIAL)

- Area: audit read-only untuk kesiapan BotConnector Web First sebagai controlled public beta. Source, konfigurasi, Basic Auth staging, deployment, production services, DNS, website utama, Local Core architecture, dan worktree MSIX tidak diubah. Tidak ada commit, push, atau pengumuman peluncuran.
- File terdampak dalam tugas ini: `WORKTREE_NOTES.md` saja. Tidak ada file/artifact release baru.
- Source dan regresi Phase 6A: `npm check` PASS (22/22), `cargo test` PASS (29/29), `cargo build` PASS, `npm run web:build` PASS, dan `git diff --check` PASS. Pemeriksaan bundle tidak menemukan pola kredensial provider; service worker v4 tetap melewati cache untuk route `/api/` dan `/v1/`. PWA installability telah lulus pada staging sebelumnya; pemasangan PWA native tetap acceptance manual.
- Staging dipertahankan: `https://app-preview.botconnector.id/` HTTP 200; tiga service staging aktif dan enabled. Website utama `https://botconnector.id/` HTTP 200 dan tidak disentuh. Browser staging Phase 6A sebelumnya mencatat model Cloud dan chat `STAGING-CLOUD-FINAL-OK` berhasil dengan Local Core berhenti; penerimaan Local Core/Spark/Unload tercatat pada entry Phase 6A.2 sebelumnya.
- Production Web belum tersedia: `app.botconnector.id` tidak memiliki DNS jawaban (NXDOMAIN); tidak ditemukan production app vhost, service `botconnector-web-app-*`, direktori release `/opt/botconnector-web-app`, atau target rollback. HTTPS, headers, PWA, auth, serta acceptance production belum bisa diverifikasi.
- Audit auth menemukan sistem akun BotConnector pusat dan endpoint session-check yang dipakai oleh panel lama, tetapi kontrak session/cookie pada subdomain `app.botconnector.id` dan pemetaan sesi ke Cloud API belum terbukti. Cloud API yang ada masih meminta Bearer API key. Tidak ada session layer Web atau cookie baru yang dibuat.
- Tidak ada allowlist model Web publik yang disahkan. Empat model Cloud enabled dan satu model disabled di database; semua row yang diaudit berstatus billing disabled dan harga nol. Request ledger mencatat token/status/latency tetapi cost tidak tersedia. `rpm_limit` dan `concurrency_limit` dibaca tetapi tidak ditegakkan di handler yang diaudit; batas output/context, budget/spend, dan rate/concurrency untuk Web belum dibuktikan. `auto-free` hanya kandidat untuk evaluasi kapasitas, bukan model yang disetujui untuk beta.
- Perlindungan prompt/log perlu ditinjau sebelum beta: jalur Rust yang diaudit menulis body chat ke log debug; hitungan pola rahasia berkeyakinan tinggi pada log cloud API 60 menit terakhir adalah 0, bukan jaminan menyeluruh untuk seluruh log. Provider secret tetap server-side; scan bundle frontend tidak menemukan secret.
- Pemeriksaan loopback read-only atas `botconnector-cloud-api.service`: systemd melaporkan active/running dengan PID, tetapi tidak ditemukan listener pada port 18220-18229, termasuk port yang dipakai konfigurasi Nginx; request ke 18220 sebelumnya ditolak. Service tidak direstart atau diubah. Konektivitas service/provider ini perlu diklasifikasi sebelum produksi.
- Dokumen public yang perlu disiapkan dan ditinjau: Privacy Policy AI/cloud (prompt, attachment, history, routing dan retensi), Terms of Service penggunaan AI/cloud, serta disclosure provider pihak ketiga dan web search. Halaman privacy/terms yang ada belum menjawab alur data AI Web First secara spesifik; tidak ada klaim hukum baru yang ditambahkan.
- Rencana sinkronisasi website setelah cloud production acceptance: jadikan Open BotConnector Web CTA utama; desktop/Local Core sekunder dan signed/MSIX menyusul; jelaskan Local AI dan PWA opsional; sinkronkan docs Cloud/Runtime/Chat/API/MCP/CLI, provider/model allowlist, auth, search/attachment, biaya/fair-use, retensi data, status layanan, dan versi/release berdasarkan bukti aktual. Homepage, Download, docs, atau domain utama tidak diubah.
- Hasil gate: `PHASE6B_RESULT=PARTIAL`; `WEB_CLOUD_PUBLIC_READY=NO`; `WEB_LOCAL_PUBLIC_READY=NO` (tetap preview). Alasan: belum ada DNS/TLS/vhost/service/release produksi; batas auth/session dan cost/abuse enforcement belum siap atau belum terverifikasi.

### 2026-09-16 - Phase 6A cloud staging gate completed

- Scope: verification-only continuation in the canonical `C:\Users\farij\Projects\BotConnector-AIChat` worktree. Basic Auth, application source, deployment, Local Core architecture, and MSIX worktree were not changed. No commit, push, or public launch.
- In the already authenticated staging browser, `GET https://app-preview.botconnector.id/v1/models` returned HTTP 200 and listed `ollama:deepseek-v4-flash:cloud`. The selected model was Deepseek V4 Flash; Inspector showed Core `Web · gateway same-origin`, Inference `Cloud`, provider `Ollama`, and model ID `ollama:deepseek-v4-flash:cloud`.
- With the canonical source/dev Local Core process stopped and port 18764 released, the exact prompt `Balas hanya: STAGING-CLOUD-FINAL-OK` returned exactly `STAGING-CLOUD-FINAL-OK`. This confirms the staging cloud chat succeeds without Local Core. Screenshot: `C:\Users\farij\Downloads\botconnector-phase6a-cloud-final.png`.
- Final infrastructure verification: `https://botconnector.id/` returned HTTP 200; `https://app-preview.botconnector.id/` returned HTTP 200 in the browser; `botconnector-website.service`, `botconnector-web-preview-core.service`, and `botconnector-web-preview-web.service` were all active and enabled.
- Verification-only task: no application test/build suite was rerun because source was not changed; the accepted source suites are recorded in the preceding Phase 6A.2 entry. `git diff --check` was run for the current worktree. This note is the only file changed by this gate; no new release/version or deployment artifact was created.

### 2026-09-16 - Phase 6A.2 staging recovery and HTTPS Local Core acceptance (PARTIAL)

- Continued in canonical worktree `C:\Users\farij\Projects\BotConnector-AIChat`, branch `main`, starting HEAD `82976d349ad97ac9aae0655ad631dace5e2a6385`. Existing dirty Phase 1-6 work was preserved; no commit, push, main-site deployment, MSIX worktree change, or public launch.
- Reverified the accepted Phase 6A.1 source behavior: only the exact official Web origins `https://app-preview.botconnector.id` and `https://app.botconnector.id` are trusted by default; arbitrary origins remain denied; Local Core inspector state is independent of gateway state; successful Local Core health initializes the local workspace before the independent model refresh; `/v1/models` retains its 10-second timeout. `assets/botconnector/app.js` was not modified in this phase.
- Added a static shell cache-version regression assertion and advanced the generated service-worker cache from `botconnector-web-shell-v3` to `botconnector-web-shell-v4` in `web/web.test.cjs` and `web/build.cjs`. This ensures existing preview service workers activate the accepted current Web shell instead of serving cached pre-fix JavaScript.
- Built the current `dist/web` and deployed a new versioned preview release at `/opt/botconnector-web-preview/releases/botconnector-web-preview-20260916T114313Z`. The immediate rollback release `/opt/botconnector-web-preview/releases/botconnector-web-preview-20260916T113756Z` remains present; `current` points to the new release. Deployment changed only the preview Web static artifact; preview Core runtime and production services were not changed.
- VPS checks: main website, preview Core, and preview Web services are all active and enabled. Preview Web loopback root and `app.js` return 200; preview Core `/v1/models` returns 200. `sudo nginx -t` succeeds; it reports pre-existing duplicate `olahdokumen.com` server-name warnings, left untouched. Public `https://botconnector.id/`, preview root, `app.js`, manifest, and service worker return 200.
- Windows Local Core source/dev health at `http://127.0.0.1:18764/api/botconnector/local/health` returns HTTP 200 `{"available":true}`. Exact preview Origin receives HTTP 200 and its exact `Access-Control-Allow-Origin`; `https://unrelated.example` receives HTTP 403. No environment override was used.
- HTTPS Local Core acceptance passed in the preview Web UI. In the task-scoped Chrome 153 profile, the exact preview origin was allowed for Chrome's “Jaringan lokal” and “Aplikasi di perangkat” permissions. Clicking `Hubungkan Local Core` transitioned through CONNECTING to Connected, with Inspector Core `Lokal · BotConnector Core` and Inference `On-device`, even while the cloud gateway model-list request was unauthenticated. This verifies Local Core connection is not blocked by cloud model refresh.
- The already installed `XHToken/Spark-X2.5-4B-GGUF` `Q4_K_M` model was run from its existing local path without redownloading; UI showed STARTING then READY. Chat prompt `Balas hanya: STAGING-LOCAL-OK` returned exactly `STAGING-LOCAL-OK`, with local/on-device Inspector state. HTTPS Unload returned Runtime to STOPPED, restored the Installed `Run` action, and Chat did not mark Spark READY.
- Cloud staging chat regression `STAGING-CLOUD-RECOVERY-OK` remains unverified in this fresh browser because the staging Basic Auth credential was not available to this session. Unauthenticated public `/v1/models` returns HTTP 401 as expected for the protected route. Basic Auth was not removed or weakened; no credential or secret value was inspected or recorded. This is an acceptance blocker, not evidence of a cloud backend regression.
- Verification: `npm run check` passed 22/22 Node tests; `cargo test` passed 29/29; `cargo build`, `npm run web:build`, and final `git diff --check` passed. No BotConnector release/version tag was created; staging artifact/release name is `botconnector-web-preview-20260916T114313Z`.
- Files changed for this phase: `web/build.cjs`, `web/web.test.cjs`, and this note. Existing Phase 1-6 dirty source files remain untouched by the cache change. Web archive: `C:\Users\farij\Downloads\botconnector-web-preview-20260916T114313Z-web.tar.gz` (SHA-256 `3DADFC287F8B5462C3B8F0B47A6F5FB0B2A256A91BD180527A4FC31F0007868A`). Acceptance screenshots are in `C:\Users\farij\Downloads\`: `botconnector-phase6a2-local-network-permission.png`, `botconnector-phase6a2-https-local-connected.png`, `botconnector-phase6a2-https-local-ready.png`, `botconnector-phase6a2-https-local-chat.png`, `botconnector-phase6a2-https-local-unload.png`, `botconnector-phase6a2-https-local-unload-chat.png`, and `botconnector-phase6a2-basic-auth-challenge.png`.

## Lokasi dan dasar

- Worktree ini: `C:\Users\farij\Projects\BotConnector-AIChat`.
- Repo BotConnector lama tetap terpisah di `C:\Users\farij\Projects\BotConnector`.
- Source dasar: `sigoden/aichat`, branch lokal `main`, commit dasar saat ini `82976d3` (`chore: update models.yaml`, 2026-02-23).
- Remote yang ada hanya `upstream`, ke `https://github.com/sigoden/aichat.git`. Belum ada remote `origin` BotConnector.
- Perubahan produk di worktree belum di-commit. Karena tidak ada commit produk per perubahan, Git tidak dapat memastikan urutan edit historis dengan tepat.

### 2026-09-16 - Phase 6A.2 staging 502 recovery attempt (VPS_ACCESS_BLOCKED)

- Read-only verification in the canonical worktree at `C:\Users\farij\Projects\BotConnector-AIChat`, branch `main`, HEAD `82976d349ad97ac9aae0655ad631dace5e2a6385`. Existing dirty Phase 1-6 files were preserved; no source, UI, configuration, dependency, deployment, or release files were changed.
- Public checks from Windows: `https://app-preview.botconnector.id/` returned HTTP 502 and `https://app-preview.botconnector.id/app.js` returned HTTP 502.
- Retried the existing SSH path `botadmin@103.58.101.207` in batch mode; SSH returned `Permission denied (publickey)`. Per task instructions, VPS investigation stopped here. Nginx, systemd, upstreams, logs, release symlink, environment names, and loopback health were not inspected. The 502 root cause is unknown and is not inferred from the public response alone.
- Verification: `git diff --check` passed with existing LF/CRLF notices. `npm run check`, `cargo test`, `cargo build`, and `npm run web:build` were not run because no canonical source changed and VPS access blocked staging recovery. No artifact or version was created; no commit, push, restart, reload, or publish occurred.

### 2026-09-16 - Phase 6A.1 Local Core state decoupling (PARTIAL)

- Scope: continue from HEAD `82976d349ad97ac9aae0655ad631dace5e2a6385` in the canonical `BotConnector-AIChat` worktree. The Phase 1-6 changes were already dirty before this task and were preserved. No commit, push, publish, production-site change, or MSIX worktree change.
- Verified pre-existing source patches: exact official Local Core origins `https://app-preview.botconnector.id` and `https://app.botconnector.id` remain in `desktop/local/web-server.cjs`; exact-origin tests remain in `desktop/local/web-server.test.cjs`; development overrides remain additional exact origins in `desktop/main.cjs`; `/v1/models` keeps its 10-second timeout in `assets/botconnector/app.js`.
- Root cause found in the UI: the connection path already marked Local Core connected after health and launched `loadModels()` without awaiting it, but the inspector was rendered only from cloud-gateway state. When the cloud gateway failed, the inspector incorrectly showed Core disconnected even though Local Core health had succeeded. The Local Core button and inspector therefore disagreed.
- `PHASE6A1_CHANGE`: `assets/botconnector/app.js` now renders the inspector Core state from Local Core health when connected, independently of cloud-gateway state. Cloud status remains separately visible in the gateway indicator. The independent model refresh remains fire-and-forget; its own error handler reports gateway failure, and the no-op promise catch was removed so unexpected errors are not swallowed.
- Local source acceptance: port 18764 was owned by this worktree's `node_modules/electron/dist/electron.exe . --local-service`; `/api/botconnector/local/health` returned HTTP 200 `{"available":true}`. A fresh source Web session connected through the UI while the cloud gateway was offline; the inspector showed `Tersambung`, `Lokal · BotConnector Core`, and `On-device`, and the Installed page opened. Screenshot: `C:\Users\farij\Downloads\botconnector-phase6a1-local-source-inspector-open.png`.
- Origin security: the running source Local Core returned HTTP 200 and `Access-Control-Allow-Origin: https://app-preview.botconnector.id` for that exact Origin; `https://unrelated.example` received HTTP 403. The automated exact-origin suite also passed. No wildcard or arbitrary BotConnector subdomain was added.
- Existing model audit: `XHToken/Spark-X2.5-4B-GGUF` `Q4_K_M` is installed at the existing user model directory (GGUF size 2,600,224,352 bytes). `/running` was empty and `/v1/models` reported Spark `unloaded`; no model download or run was performed.
- Staging acceptance is blocked by current infrastructure: `https://app-preview.botconnector.id/` and `/app.js` returned Nginx HTTP 502 on repeated checks, so no staging Web UI, HTTPS Local Core, Spark Run/Ready/Chat/Unload, or hosted screenshot could be verified. A read-only SSH status attempt to the preview host was denied (`Permission denied (publickey)`); no staging service or artifact was changed. The main `botconnector.id` site was not touched.
- PWA remains `PWA_INSTALLABILITY=PASS` from the preceding Phase 6A verification; manual native install remains `PWA_MANUAL_INSTALL=MANUAL_ACCEPTANCE_PENDING`. This task found no PWA bug.
- Verification after the source change: `npm run check` passed 21/21 Node tests, `cargo test` passed 29/29, `cargo build` succeeded, `npm run web:build` succeeded, and `git diff --check` found no whitespace errors (existing LF/CRLF warnings only). Local UI and origin checks above passed. Package metadata remains `0.2.0`; no BotConnector release or new published artifact is claimed.
- Files changed in this task: `assets/botconnector/app.js`, `WORKTREE_NOTES.md`. Generated Web output is under ignored `dist/web`; no new tracked files were created.

### 2026-09-16 - Phase 6A HTTPS staging deployment preparation (PARTIAL)

- Scope: prepare the accepted Web First artifact for app-preview.botconnector.id without touching the main botconnector.id website or the separate MSIX worktree. No commit, push, publish, or version change.
- Source baseline: canonical worktree C:\Users\farij\Projects\BotConnector-AIChat, branch main, HEAD 82976d349ad97ac9aae0655ad631dace5e2a6385, with pre-existing Phase 1-5 work still dirty. Final checks passed: npm check 18/18, cargo test 29/29, cargo build, npm run web:build, git diff --check.
- Read-only hosting audit: VPS Ubuntu 24.04, nginx 1.24, Certbot/Let's Encrypt. Existing site root /opt/botconnector-web/current and botconnector-website.service were left untouched. Existing TLS certificate covers botconnector.id and www.botconnector.id only. Main site still returned HTTPS 200 after staging preparation.
- DNS audit: botconnector.id is served by aster.dns-parking.com and helios.dns-parking.com; app-preview.botconnector.id has no A record. hpanel.hostinger.com presented a Cloudflare security challenge in the isolated browser, and no DNS management connector was available. No DNS record or TLS certificate was changed or issued.
- Port audit found 127.0.0.1:8000 belongs to the existing TradingView Webhook Bridge, not BotConnector Core; it was not reused or modified. A dedicated Rust BotConnector backend was built from this dirty canonical source in an isolated rust:1.97-bookworm container and runs at 127.0.0.1:18440. A separate Node Web gateway runs at 127.0.0.1:18808. Both ports are loopback-only and have dedicated systemd units.
- Versioned artifact: C:\Users\farij\Downloads\botconnector-web-preview-20260916T054053Z.tar.gz. SHA-256 2F5FACC8EB21EEFF228B6DF706E502D062153036691BEFA98AAE1101C65FAFE2. It contains the final dist/web (8 files), web/server.cjs, Linux x86_64 BotConnector 0.2.0 backend, deployment templates, and upstream notices. It contains no .env, provider key, user model, node_modules, Electron/MSIX binary, or certificate. All 8 static asset hashes matched the final npm run web:build output; remote archive hash matched.
- Artifact audit: 26 entries; zero secret-value matches and zero forbidden entries. The Exa key was transferred only over SSH to /etc/botconnector-web-preview/staging.env (root:botadmin, mode 0640); sanitized server config contains only the Ollama cloud client and excludes the unrelated OpenAI key. No secret value was printed or added to the artifact.
- Private gateway checks: web root 200, /v1/models 200 with ollama:deepseek-v4-flash:cloud, local-management path 404. The dedicated gateway returned STAGING-CLOUD-OK. A single Exa test query returned HTTP 200, provider Exa, one BMKG result. These requests used the VPS loopback endpoint, not the public hostname.
- Browser smoke used an isolated Chromium session through an SSH tunnel to http://127.0.0.1:18808 (not HTTPS). Chat/cloud response STAGING-CLOUD-OK, small TXT attachment selection and submission STAGING-ATTACHMENT-OK, Models/Installed showed Local Core disconnected, Tools and Developer rendered, manifest loaded, and service worker registered. At 390px the document width remained 390px and the composer remained visible. Browser console and page errors were empty.
- Actual HTTPS/browser, public Basic Auth, Let's Encrypt certificate, redirect, PWA install, public rate limiting, and Local Core/Spark through the exact staging origin remain untested because staging DNS is absent. The Nginx Basic Auth/rate-limit/TLS templates are in the artifact but were not activated. No nginx reload, production service change, DNS mutation, or public exposure occurred.
- Staging release is installed under /opt/botconnector-web-preview/releases/botconnector-web-preview-20260916T054053Z with current pointing to that release; its API and Web services are enabled but only bind to loopback. To finish, add DNS A app-preview -> 103.58.101.207 (do not add AAAA), then provision Let's Encrypt and activate the protected Nginx vhost. Rollback before public activation is to stop/disable the two preview units and remove the preview-only release/config; the main website is separate.
- Source code was not changed for Phase 6A; this entry records the staging artifact, private service setup, tests, and remaining DNS-dependent acceptance. No commit was created.

### 2026-09-16 - Phase 6A continuation after DNS (PARTIAL)

- DNS verified independently against Cloudflare (1.1.1.1), Google (8.8.8.8), and Hostinger authoritative DNS: app-preview.botconnector.id A = 103.58.101.207. No AAAA address was returned.
- The existing preview release was used; source was not rebuilt or redeployed. Certbot issued a trusted Let's Encrypt certificate for app-preview.botconnector.id (valid 2026-09-16 through 2026-12-15). HTTP redirects to HTTPS; the existing botconnector.id website remained HTTP 200 and botconnector-website.service stayed active.
- Activated an isolated Nginx preview vhost, dedicated Basic Auth file, API security headers, and per-IP chat/search rate zones. Public static shell/PWA files are intentionally unauthenticated so the browser can register its service worker; cloud/model/search API routes require Basic Auth. Unauthenticated /v1/models and web-search requests returned 401, authorized model requests returned 200, and 15 of 20 rapid malformed chat requests were rate-limited with 429. /api/botconnector/local/* remains 404 at the public gateway. No provider secret appeared in staging service logs.
- HTTPS browser acceptance: Web shell, cloud model list and chat (STAGING-CLOUD-OK and STAGING-CLOUD-PWA-OK), one Exa Search API documentation lookup with source results, and small TXT attachment (response ATTACHMENT-OK) all passed. Developer and Tools pages loaded. Local-unavailable UI rendered helpfully. At 1366, 1024, and 390px viewport widths, document width equaled viewport width; composer remained usable at 390px.
- PWA checks on the final hosted build: manifest/icon/service-worker endpoints returned 200; manifest MIME and standalone/start/scope metadata were correct; secure context and install prompt event were confirmed; service worker registered at https://app-preview.botconnector.id/ and reached activated state. The actual browser install and standalone launch were not performed pending the required user confirmation for a browser installation action.
- Windows Local Core initially ran as the installed BotConnector 0.2.0 app with Spark loaded. Its origin guard returned 403 for https://app-preview.botconnector.id and also rejected an unrelated public origin, confirming default-deny behavior. The allowlist is read at process startup from BOTCONNECTOR_ALLOWED_WEB_ORIGINS. Local Core later stopped; a tool-policy rejection blocked relaunch with the temporary staging origin, so HTTPS-to-Local-Core and Web Run/Ready/Chat/Unload remain incomplete. With the Core process confirmed stopped, HTTPS cloud chat still returned STAGING-CLOUD-CORE-OFF-OK, and Installed/Runtime displayed disconnected/stopped states. No environment variable or persistent allowlist was written.
- Static shell, PWA script, and API paths were verified separately: root is public static content, inference/search APIs require Basic Auth, cache-control on chat/models is no-store, and CSP/nosniff/referrer/permissions/HSTS headers were observed on HTTPS. Staging Basic Auth credential is held outside this note and artifact; static shell is public but inference quota endpoints remain protected and rate-limited.
- Actual HTTPS screenshots are in C:\Users\farij\Downloads\: botconnector-phase6a-https-cloud-chat.png, botconnector-phase6a-https-cloud-core-off.png, botconnector-phase6a-https-attachment.png, botconnector-phase6a-https-developer.png, botconnector-phase6a-https-local-unavailable.png, botconnector-phase6a-https-installed-core-off.png, botconnector-phase6a-https-runtime-core-off.png, botconnector-phase6a-https-tools.png, botconnector-phase6a-https-mobile.png, and botconnector-phase6a-https-pwa-shell.png. These are not screenshots of an installed standalone PWA.
- No source files, production-site files, or MSIX worktree files were changed. No commit, push, version bump, or public launch occurred. Nginx rollback backup is /etc/nginx/sites-available/botconnector-web-preview-https.conf.before-pwa-static-20260916; the ACME-only vhost is preserved in /etc/nginx/sites-available/botconnector-web-preview-acme.conf. The staging services and release are isolated from the production site.

### 2026-09-16 - Phase 6A continuation: auth rotation and remaining acceptance (PARTIAL)

- Continued on the existing staging release; no source rebuild/redeploy. DNS remains app-preview.botconnector.id -> 103.58.101.207. HTTP returns 301 to HTTPS; HTTPS returns 200 with curl certificate verification 0. The main https://botconnector.id/ still returns 200.
- Rotated the staging Basic Auth password at the user's request. Old credential now returns 401 and new credential returns 200 for /v1/models. The new password is not recorded in this note, artifact, or final report. Nginx config test and reload succeeded; htpasswd remains root:www-data mode 0640.
- Final-host PWA checks: manifest 200 (application/manifest+json), SVG icon 200, /sw.js 200, manifest start_url and scope are ./ and display is standalone; in the isolated Chrome 153 profile the service worker registration is activated at scope https://app-preview.botconnector.id/. Web chat after rotation returned STAGING-CLOUD-OK with Local Core stopped.
- User authorized PWA installation and standalone launch. beforeinstallprompt was observed and prompt() invoked, but the native browser install confirmation was not accessible: CUA exposed no browser/app surface; Chrome's CDP endpoint lacks PWA.install; chrome://web-app-internals still showed no installed web app. Therefore actual installation, installed-app listing, and standalone launch are NOT PASS and are not claimed. A real cloud screenshot is C:\Users\farij\Downloads\botconnector-phase6a-cloud-auth-rotated.png.
- The installed package BotConnectorAI.Dev was launched once through the existing botconnector-aichat.exe alias with BOTCONNECTOR_ALLOWED_WEB_ORIGINS set only to https://app-preview.botconnector.id. The invocation exited -1; port 18764 became unavailable and 11435 was not listening. No alternate launch mechanism was tried. Therefore HTTPS-to-Local-Core, staging-origin allow, Run/Ready, STAGING-LOCAL-OK, Unload, and live unrelated-origin rejection remain NOT TESTED. Public hosted /api/botconnector/local/health still returns 404.
- No production site or MSIX worktree was touched. No commit, push, publish, or version change. Staging remains protected and rollback configuration is unchanged.

### 2026-09-16 - Phase 5 Web First foundation (working tree)

- Scope: menambahkan track Web terpisah dari Electron/Desktop. Desktop `desktop/`, runtime manager, tools/MCP, dan installer tidak dipindahkan atau dihapus. Tidak ada perubahan website produksi, port canonical, versi, commit, push, atau publish.
- `PHASE5_CHANGE`: `web/build.cjs`, `web/server.cjs`, `web/web.test.cjs`, `assets/botconnector/index.html`, `assets/botconnector/app.js`, `assets/botconnector/app.css`, `desktop/local/web-server.cjs`, `desktop/local/web-server.test.cjs`, `desktop/main.cjs`, `package.json`, `README.md`, dan catatan ini. Perubahan dirty Rust/core serta Phase 1–4 dipertahankan sebagai `PREEXISTING_CHANGE`.
- Web build menghasilkan `dist/web` yang hanya berisi shell statis, konfigurasi platform Web, manifest PWA, icon SVG, service worker static-only, serta `security-headers.json`; tidak menyalin Electron/desktop dan tidak memuat secret provider. `npm run web:build` dan `npm run web:dev` menjadi workflow eksplisit.
- `web/server.cjs` adalah gateway same-origin sederhana: hanya meneruskan `/v1/models`, `/v1/chat/completions`, embeddings/rerank, health, dan web search/fetch ke gateway Rust melalui `BOTCONNECTOR_WEB_GATEWAY`; `/api/botconnector/local/*` ditolak (local management tetap milik Local Core). Tidak ada `child_process` di Web server/bundle.
- UI memakai `window.BOTCONNECTOR_CONFIG` dari `config.js`. Mode Web tidak membuat local API pada boot, sehingga Cloud berjalan tanpa Local Core. Tombol `Hubungkan Local Core` melakukan probe eksplisit ke endpoint konfigurasi (`http://127.0.0.1:18764` default), memetakan CONNECTING/CONNECTED/UNAVAILABLE/PERMISSION_REQUIRED, lalu mengaktifkan adapter API lokal yang sama tanpa menduplikasi UI. Onboarding Web menampilkan `Mulai dengan Cloud` dan `Hubungkan Local AI`.
- Desktop local web server tetap menjadi adapter native; allowlist CORS dapat diperluas melalui `BOTCONNECTOR_ALLOWED_WEB_ORIGINS` dan mengirim header origin yang tepat untuk koneksi Web Local Core. Origin asing tetap ditolak.
- PWA shell memakai manifest `display: standalone`, scope/start URL relatif, icon SVG BotConnector, dan service worker yang hanya cache aset statis (tidak cache API/chat/secrets). Base path `/app/` dilayani oleh Web server melalui path relatif.
- Acceptance live: shell Web boot saat gateway offline (tanpa fatal error), cloud model list dan chat `WEB-CLOUD-OK` lulus melalui gateway Rust, Local Core tersambung setelah klik eksplisit dan state `Connected` terlihat. Runtime/model lokal tidak dapat dijalankan pada sesi ini karena model/runtime lokal tidak aktif; tidak ada redownload. Desktop Electron dibuka dengan remote debugging, shell dan halaman Tools tampil, lalu ditutup sehingga port 18763/18764 dilepas.
- Verifikasi otomatis: `npm run check` mencakup 18 test Node (Phase 1–4 + Web build/security/route tests), `cargo test` 29/29, `cargo build`, `npm run web:build`, dan `git diff --check` dijalankan. Screenshot aktual disimpan ke `C:\Users\farij\Downloads\`: `botconnector-phase5-web-cloud.png`, `botconnector-phase5-web-offline-chat.png`, `botconnector-phase5-web-local-unavailable.png`, `botconnector-phase5-web-developer.png`, `botconnector-phase5-web-tools.png`, `botconnector-phase5-web-local-connected.png`, dan `botconnector-phase5-desktop-regression.png`.
- `WEBSITE_SYNC_BACKLOG`: website belum diubah; sinkronisasi berikutnya perlu mencatat onboarding Web-first, gateway cloud same-origin, Local Core optional/LNA permission, endpoint dinamis, dan batas public-vs-local management. `PHASE6_BACKLOG`: production deployment/staging, auth/accounts, billing/quotas, remote/LAN Core, remote/OAuth MCP, signed installer, dan native mobile.

### 2026-09-16 - Phase 5.1 deterministic Web build closeout

- EPERM direproduksi sebelum perubahan: `unlink` pada `C:\Users\farij\Projects\BotConnector-AIChat\dist\web\app.css`, errno `-4048`. `attrib` dan ACL tidak menunjukkan file READONLY; owner/ACL memberi Modify. Port 8080 saat diagnosis dimiliki `pnlclaw-server`, bukan proses BotConnector, sehingga tidak dibunuh.
- `web/build.cjs` kini menulis seluruh artefak ke `dist/.web-build-<pid>-<timestamp>`, memakai `fs.rm` bounded retry (`maxRetries=5`, `retryDelay=150`), mempromosikan dengan rename retry untuk EPERM/EBUSY/ENOTEMPTY, dan membersihkan backup generated setelah sukses. Sumber, `.git`, model storage, serta permission source tidak dimutasi (`SOURCE_PERMISSION_MUTATION_COUNT=0`).
- Atomicity diuji secara praktis saat Web server aktif: tiga `npm.cmd run web:build` beruntun semuanya PASS tanpa EPERM; artifact lama tetap disajikan sampai staging siap. `EPERM_COUNT=0` pada acceptance setelah perbaikan.
- Artifact audit final: `dist/web` berisi `index.html`, `app.css`, `app.js`, `config.js`, manifest, icon, `sw.js`, dan security headers; tidak ada `.env`, provider key, node_modules, Electron, atau `child_process`. Subpath `/app/` dan manifest/icon/service-worker mengembalikan HTTP 200.
- Local Core acceptance: Desktop track dijalankan melalui `npm.cmd run desktop` dengan allowlist origin Web. Spark `XHToken/Spark-X2.5-4B-GGUF` `Q4_K_M` dijalankan dari path terpasang (tanpa redownload), status runtime READY dan health API 11435 true. Local chat melalui adapter API mengembalikan tepat `WEB-LOCAL-OK`; unload mengembalikan lifecycle STOPPED. Browser automation tidak tersedia setelah environment reset, jadi klik UI Web untuk Run/response tidak diulang; endpoint yang dipakai adalah endpoint yang sama dengan adapter Web.
- Desktop regression final: Electron shell berhasil start bersama Core 18763, local web 18764, llama-swap/llama-server 11435; Chat, Installed, Runtime, dan Tools terlihat. Semua proses BotConnector dihentikan setelah acceptance.
- Final checks: `npm.cmd run check` 18/18, `cargo test` 29/29, `cargo build`, `npm.cmd run web:build`, rebuild tambahan, dan `git diff --check` PASS. Tidak ada commit/push/deploy/version bump. `MSIX_WORKTREE_TOUCHED=NO`.
- `PHASE6_BACKLOG`: deployment staging/production, auth/accounts, billing/quotas, remote Core, remote/OAuth MCP, signed installer, mobile, dan website synchronization. `WEBSITE_SYNC_BACKLOG` tetap memuat perbedaan onboarding Web-first, endpoint/configuration, dan batas public-vs-local management.

## Riwayat yang dapat dibuktikan

### 2026-09-16 - Phase 4 native tools + MCP integration

- Scope: menambahkan permukaan Tools/MCP kecil dengan memanfaatkan function calling AIChat yang sudah ada. Tidak ada perubahan website, port API, versi, atau commit/push/publish. Semua perubahan Phase 4 dipisahkan dari perubahan dirty sebelumnya.
- Audit source: function calling native ditemukan di `src/function.rs`, `src/config/input.rs`, `src/client/common.rs`, dan route OpenAI-compatible di `src/serve.rs`. Tidak ditemukan implementasi MCP upstream yang bekerja; adapter lokal Phase 4 memakai transport stdio minimal. Tidak menambahkan dependency Cargo `llm-functions` atau framework MCP baru.
- `PHASE4_CHANGE`: `desktop/local/tools.cjs`, `desktop/local/mcp-fixture.cjs`, `desktop/local/tools.test.cjs`, `desktop/main.cjs`, `desktop/local/web-server.cjs`, `assets/botconnector/index.html`, `assets/botconnector/app.js`, `assets/botconnector/app.css`, `package.json`, dan catatan ini. `PREEXISTING_CHANGE` pada Rust/core, packaging, dan aset lain tetap dipertahankan.
- Registry kanonik `ToolRegistry` menyediakan `get_current_time` dan `echo_text` dengan default DENY, schema validation, permission class READ/WRITE/EXECUTE, timeout 10 detik, maksimum 4 panggilan per turn, activity event, serta penolakan unknown/disabled/malformed tool. Tidak ada unrestricted shell atau write/delete tool.
- MCP lokal memakai `command + args[]`, fixture stdio JSON-RPC, discovery `tools/list`, namespacing tool, explicit allowlist, status STARTING/READY/FAILED/STOPPED, timeout, failure isolation, dan shutdown hanya untuk child process milik BotConnector. Default fixture memakai executable `node` agar kompatibel dengan Electron.
- UI: halaman Tools menampilkan registry dan MCP status, checkbox explicit enablement, tombol composer `Tools · N aktif`, activity block di chat, dan Developer facts yang factual. Model capability dibedakan Supported/Unsupported/Unknown. Chat mengirim tools hanya bila model mendukung; tool loop tetap dibatasi.
- Koreksi live: native tool call sempat terhapus ketika web search OFF karena filter lama mengosongkan seluruh `tool_calls`; filter kini hanya membuang `web_search`/`web_fetch`, sehingga function tool tetap berjalan. GPT-5 request menghilangkan `temperature` yang tidak didukung.
- Acceptance live: `openai:gpt-5-nano` mengeluarkan tool call terstruktur, `get_current_time` dieksekusi lewat registry, hasil dikirim kembali, jawaban final tampil, dan activity block terlihat. Disabled builtin ditolak HTTP 400. MCP fixture READY, discovered `mcp_local-fixture_echo_text`, allowlist/invocation mengembalikan `phase4-mcp`; bad command menjadi FAILED tanpa merusak Chat/Runtime. Screenshot aktual dipindahkan ke `C:\Users\farij\Downloads\`: `botconnector-phase4-tools.png`, `botconnector-phase4-mcp.png`, `botconnector-phase4-developer.png`, `botconnector-phase4-tool-activity.png`, dan `botconnector-phase4-cloud-inspector.png`.
- Verifikasi otomatis final: `node --test desktop/local/tools.test.cjs` 7/7, `npm run check` 15/15, `cargo test` 29/29, `cargo build` berhasil, dan `git diff --check` tidak menemukan whitespace error (hanya peringatan line-ending LF/CRLF pada file dirty lama). Desktop dijalankan melalui Electron, lalu dihentikan dengan Ctrl+C; port 18764/18763/11435 dan proses owned BotConnector sudah dilepas. Tidak ada commit/push/version bump.
- `WEBSITE_SYNC_BACKLOG`: dokumentasi website yang menyebut MCP/alat lebih luas dari implementasi aplikasi perlu diselaraskan; aplikasi saat ini hanya MCP lokal stdio dengan allowlist explicit, belum remote/OAuth. Endpoint UI desktop tetap 18764/core 18763 dan runtime inference 11435 sesuai konfigurasi aplikasi.
- `PHASE5_BACKLOG`: remote MCP, OAuth, unrestricted shell/agent loop, Responses/Anthropic API, SDK, remote serving, billing/auth, dan perubahan website.

### 2026-09-16 - Phase 3 runtime resilience + live state synchronization

- Scope: memperkuat lifecycle lokal Phase 2 agar perpindahan model, pemulihan helper/runtime, deteksi crash, dan sinkronisasi UI memakai satu kebenaran runtime. Tidak ada perubahan port UI 8000, tidak ada perubahan website, tidak ada kenaikan versi, dan tidak ada commit/push.
- `PREEXISTING_CHANGE`: seluruh perubahan Rust/core, desktop/local service, packaging Electron, aset BotConnector, dan catatan yang sudah dirty sebelum fase ini dipertahankan. `PHASE3_CHANGE`: `desktop/local/llama-swap.cjs`, `desktop/local/llama-swap.test.cjs`, `assets/botconnector/app.js`, `assets/botconnector/index.html`, serta loop supervisor di `src/serve.rs`.
- State kanonik tetap `LlamaSwap.lifecycle`. Konsumennya adalah endpoint runtime-status, event SSE `runtime:status`, polling UI 3 detik sebagai fallback, halaman Installed/Runtime, picker Chat, dan inspector. Status direkonstruksi dari `/running` serta `/v1/models`; READY hanya setelah health API 11435 terverifikasi.
- Switch sekarang memakai transaksi `SWITCHING → unload A → STARTING B → READY`. Jika B gagal, B menjadi `FAILED`; adapter mencoba memulihkan A dan hanya menandainya READY setelah warmup dan probe health berhasil. Lock mencegah Run/Unload bersamaan. Tes deterministik mencakup switch sukses, rollback, crash invalidasi READY, dan rekonstruksi setelah sidecar restart.
- Kehilangan `llama-server` atau `llama-swap` tidak lagi meninggalkan READY palsu; status menjadi FAILED dan endpoint ditampilkan sebagai `Configured endpoint`. Supervisor server Rust memantau helper Electron milik BotConnector dan menggantinya saat proses helper berhenti; shutdown terkontrol menghentikan child yang dimiliki aplikasi.
- Perubahan UI kecil: label sekunder Models yang redundan dihapus; Runtime STOPPED menampilkan endpoint terkonfigurasi; event/poll menyegarkan Installed, Runtime, Chat picker, dan inspector tanpa refresh browser.
- Acceptance live tanpa redownload: Spark Q4_K_M Run → READY, switch ke SmolVLM Q8_0 → READY, kill `llama-server` → FAILED, restart helper PID 40100 → 23460 dan UI pulih, kill `llama-swap` → FAILED lalu Spark dijalankan ulang, Unload → STOPPED, rerun Spark → READY, dan chat lokal membalas `PHASE3-OK`. `/v1/models` dan `/running` 11435 menunjukkan identity/status yang sesuai. Renderer `errors` dan `console` kosong.
- Screenshot aktual dipindahkan ke `C:\Users\farij\Downloads\`: `botconnector-phase3-installed-model-a-ready.png`, `botconnector-phase3-installed-model-b-ready.png`, `botconnector-phase3-runtime-ready.png`, `botconnector-phase3-runtime-stopped.png`, `botconnector-phase3-chat-phase3-ok.png`, dan `botconnector-phase3-cloud-inspector.png`.
- Port 8000 diperiksa tanpa listener duplikat; uji berjalan di port 18000. Shutdown server sebelumnya melepaskan listener dan proses owned; tidak ada orphan yang tersisa. CLI upstream saat ini tidak menyediakan `ps`/`unload`, sehingga shared-state GUI/CLI dicatat `NOT_APPLICABLE` dan website sync backlog memuat perbedaan tersebut, endpoint 11435, versi 0.2.0, serta istilah Run/Unload/status aktual.
- Verifikasi: `npm run check`, `cargo test`, `cargo build`, dan `git diff --check` dijalankan; tidak ada commit atau perubahan versi. Backlog berikutnya: MCP/tools/SDK, remote serving, multi-model simultan, billing/auth, dan sinkronisasi dokumentasi website.

### 2026-09-16 - Phase 2 unified local model lifecycle

- Scope: menyatukan alur model lokal menjadi **Discover → Inspect → Download → Installed → Run → STARTING → READY → Chat → Unload → STOPPED**. Fase ini tidak mengubah port UI 8000, tidak mengganti llama-swap/llama.cpp, tidak menaikkan versi, dan tidak membuat commit.
- `PREEXISTING_CHANGE`: perubahan Rust/core, packaging Electron, desktop/local service, aset BotConnector, dan catatan yang sudah dirty sebelum Phase 2 dipertahankan. `PHASE2_CHANGE`: lifecycle adapter, route guard chat, status/presentasi Installed/Runtime, CSS, dan tes deterministik yang dicatat di bawah.
- Root cause alur lama: tombol **Pakai model** hanya mengaktifkan profil llama-swap. Model belum diwarmup dan belum diprobe melalui API 11435, sehingga picker sudah memilih alias `botconnector-chat` ketika route belum siap. Ini memunculkan `no router for requested model` pada chat pertama.
- Perbaikan no-router: `desktop/local/llama-swap.cjs` kini memiliki satu state lifecycle aktif, lock transisi, Run idempotent, warmup request, polling `/running` + `/v1/models`, serta status `STOPPED/STARTING/READY/FAILED`. `desktop/main.cjs` menolak chat lokal jika state belum READY dengan pesan yang bisa ditindaklanjuti. Picker otomatis disinkronkan ke model aktif setelah READY dan ditandai STOPPED setelah Unload.
- Sumber state: `LlamaSwap.lifecycle` adalah state model lokal kanonik; `local/runtime-status` meneruskannya ke UI. Health aktual diverifikasi dari `http://127.0.0.1:11435/v1/models` dan `/running`; UI tetap berjalan di port uji 18000 selama acceptance karena listener 8000 lama sedang tersangkut.
- File Phase 2: `desktop/local/llama-swap.cjs`, `desktop/main.cjs`, `assets/botconnector/index.html`, `assets/botconnector/app.js`, `assets/botconnector/app.css`, `package.json`, dan `desktop/local/llama-swap.test.cjs` (baru). Tes mencakup state kanonik, Run idempotent, STARTING→READY, dan invalid model→FAILED.
- Verifikasi otomatis terakhir: `npm run check` lulus (5/5 Node tests); `cargo test` lulus (29/29); `cargo build` berhasil. Browser/Electron renderer `errors` dan `console` kosong.
- Acceptance live dengan `XHToken/Spark-X2.5-4B-GGUF` `Q4_K_M`: baseline STOPPED, Run dari Installed, STARTING terlihat, READY health, Chat auto-sync `PHASE2-OK`, Run idempotent (PID llama-server tetap 30192 pada pengujian), Unload STOPPED, dan rerun `PHASE2-RERUN-OK` lulus. Tidak ada `no router` pada log acceptance.
- API 11435: `/v1/models` menampilkan model Spark `loaded` saat READY dan semua model `unloaded` sesudah Unload; `/running` menampilkan satu route Spark `state: ready` saat READY dan kosong sesudah Unload. Audit proses saat READY menunjukkan satu `llama-swap`, satu `llama-server`, dan satu `botconnector` server; tidak ada proses duplikat/orphan yang tidak dijelaskan.
- Regression live: Discover menampilkan katalog 100 hasil, Downloads dapat dibuka, cloud `ollama:deepseek-v4-flash:cloud` tampil sebagai **Cloud · Ollama** sementara Core tetap lokal, toggle **Cari internet · Exa** hanya aktif setelah klik, attachment teks bisa ditambah/dihapus, dan riwayat berisi percakapan acceptance.
- Screenshot aktual dipindahkan ke `C:\Users\farij\Downloads\`: `botconnector-phase2-installed-stopped.png`, `botconnector-phase2-installed-ready.png`, `botconnector-phase2-runtime-ready.png`, `botconnector-phase2-runtime-stopped.png`, `botconnector-phase2-chat-local.png`, `botconnector-phase2-chat-cloud.png`, dan `botconnector-phase2-chat-rerun.png`.
- CLI yang tersedia tidak memiliki subperintah `ps`; bukti GUI/CLI shared state ditandai `NOT_APPLICABLE` sesuai batasan fase, tanpa membuat CLI baru. Phase 3 backlog: rollback otomatis model switch, event push lintas proses, dan evaluasi port UI 8000 yang tersangkut.

### 2026-09-16 - Phase 1 product shell parity

- Scope: menyusun ulang shell UI menjadi **◇ BotConnector AI** dengan navigasi primer Chat, Models, Tools, dan Developer; fungsi backend/model lama tetap dipakai.
- `PREEXISTING_CHANGE`: seluruh perubahan core Rust, desktop/local service, packaging, serta aset BotConnector yang sudah dirty sebelum fase ini dipertahankan. `PHASE1_CHANGE`: perubahan presentasi dan navigasi pada tiga aset UI di bawah.
- File UI yang disentuh: `assets/botconnector/index.html`, `assets/botconnector/app.css`, `assets/botconnector/app.js`.
- Perubahan perilaku: Models memiliki subnavigasi Discover, Installed, Downloads, dan Runtime; hardware fit ditempatkan sebagai konteks Discover; Tools/Developer hanya menampilkan kemampuan yang benar-benar tersedia.
- Normalisasi presentasi model dipusatkan di `normalizeModelPresentation()` untuk label ramah, ID asli, provider, lokasi inferensi, tipe, dan quantization yang tersedia.
- Inspector kanan kini memisahkan status Core dari lokasi inference. Model cloud menampilkan `Cloud · Ollama`; model lokal menampilkan `On-device` dan runtime lokal saat metadata tersedia.
- Composer mempertahankan attachment dan toggle pencarian; toggle aktif menampilkan `Cari internet · Exa` dan indikator eksternal, tanpa mengaktifkan pencarian otomatis.
- Status loading/error tetap berasal dari API aktual; kegagalan layanan lokal ditampilkan sebagai error, bukan spinner permanen.
- Verifikasi fase: `node --check assets/botconnector/app.js` lulus; `npm run check` lulus (1/1 web-server test); `cargo test` lulus (29/29). UI nyata diuji di `http://127.0.0.1:18000` karena port 8000 sedang dipakai proses lama: shell, subnavigasi model, cloud/local inspector, Tools/Developer, hardware fit kontekstual, attachment add/remove, dan toggle `Cari internet · Exa` terverifikasi.
- Direct local regression ke service aktif dengan pesan `Balas hanya: PHASE1-OK` mengembalikan `PHASE1-OK` (HTTP 200). Proxy UI pada port uji 18000 menolak operasi lokal karena service yang sedang berjalan dipercaya untuk origin canonical 8000; kondisi ini dicatat sebagai keterbatasan harness uji, bukan perubahan source.
- Build uji: `cargo build` berhasil untuk versi aplikasi saat ini `0.2.0`; tidak ada commit, push, publish, atau perubahan versi.
- Artefak screenshot uji dipindahkan dari `%TEMP%` ke `C:\Users\farij\Downloads\`: `botconnector-phase1-local-final.png`, `botconnector-phase1-cloud-final.png`, `botconnector-phase1-discover.png`, dan `botconnector-phase1-runtime.png`.
- Rerun live setelah helper lokal direstart secara graceful: Discover menampilkan 100 hasil katalog dan hardware AMD/RAM; Runtime berstatus `Siap`, llama.cpp/llama-swap/llmfit aktif, dan profil Spark X2.5 Q4_K_M tersedia. Profil Spark diaktifkan melalui UI lalu chat lokal membalas `PHASE1-OK`; browser/Electron console tetap 0 error. Screenshot baru `C:\Users\farij\Downloads\botconnector-phase1-local-success.png`.

### 2026-09-15 - artefak installer lokal

| Artefak | Waktu file lokal | Status yang diketahui |
| --- | --- | --- |
| `dist/desktop/BotConnector-0.1.0-win-x64.exe` | 21:42 | Installer lokal ditemukan; `.blockmap` juga ada. |
| `dist/desktop/BotConnector-0.1.1-win-x64.exe` | 22:02 | Installer lokal ditemukan; `.blockmap` juga ada. |
| `dist/desktop/BotConnector-0.2.0-win-x64.exe` | 22:53 | Installer lokal ditemukan; `.blockmap` juga ada. Package saat ini `0.2.0`. |

Folder `dist/desktop-portable/` berisi hasil build portable/unpacked. Artefak ini menunjukkan build pernah dibuat secara lokal. Tidak ada tag release BotConnector atau snapshot source per installer, sehingga catatan ini tidak mengarang daftar fitur untuk masing-masing versi.

Tag `v0.1.0` sampai `v0.30.0` yang ada di Git berasal dari riwayat upstream AIChat, bukan release BotConnector.

### 2026-09-16 - verifikasi dan pencatatan worktree

- Menguji chat AI lokal dengan model `XHToken/Spark-X2.5-4B-GGUF`, quantization `Q4_K_M`. Request langsung ke llama-swap sukses dengan HTTP 200; chat UI BotConnector membalas `OK`.
- Mencatat bahwa pemilih umum `Model lokal (llama-swap)` memerlukan profil model aktif. Tanpa profil, chat mengembalikan `no router for requested model`. Memilih **Model terpasang -> Pakai model** mengaktifkan profil; chat berikutnya berhasil.
- Profil aktif terakhir yang teramati: `model-xhtoken-spark-x2-5-4b-gguf-q4-k-m`.
- Bukti tes disimpan di `%TEMP%\botconnector-localai-test\report.md` dan folder `screenshots`.
- Menambahkan `AGENTS.md` sebagai aturan pencatatan dan file ini sebagai catatan berkelanjutan.
- Verifikasi: `npm run check` lulus, termasuk syntax checks dan 1/1 tes web-server. `cargo test` belum dijalankan dalam sesi pencatatan ini.
- Tidak ada perubahan source aplikasi dalam tes chat ini; profil runtime lokal diaktifkan untuk menguji jalurnya.

### 2026-09-16 - Phase 6B.2 Task 4 platform-home login handoff

- Menambahkan central login handoff pada source platform-home VPS `/opt/botconnector-platform-starter-v0.3/homepage`: `GET /app-login/start` memvalidasi state dan symbolic client, mempertahankan `app_client_id`/`app_state` melalui form login internal, lalu memanggil `/v1/app-auth/handoffs` dengan sesi central dan CSRF yang sudah ada.
- Callback hanya berasal dari metadata `callback_url` yang dikembalikan central app-auth; platform-home menambahkan code/state dengan URL utilities. Tidak ada callback URL, redirect URI, return URL, atau next dari browser yang dipakai.
- Template `app/templates/auth.html` menambahkan hidden continuation fields yang tervalidasi dan ter-escape. Normal login tanpa continuation tetap dipertahankan.
- Dokumentasi Task 4 pada `docs/superpowers/plans/2026-09-16-botconnector-phase-6b2-auth-free-cloud-implementation.md` diselaraskan dengan continuation `app_client_id` + `app_state` tanpa signed-cookie ambiguity.
- Checkpoint eksternal sebelum edit: `/home/botadmin/checkpoints/botconnector-platform-home-task4-20260916T163207Z.tar.gz`; source tidak bind-mounted read-write ke container aktif.
- Tes terfokus platform-home: 10 lulus. Regression gabungan `test_app_login_task4.py` + `test_ux_polish_regression.py`: 14 lulus. Full suite: 77 lulus, 6 gagal pada evaluasi Support AI yang tidak terkait Task 4; rerun terhadap checkpoint pra-edit mereproduksi 4 dari kegagalan tersebut dan tidak ada perubahan pada area Support AI.
- Tidak ada restart production, perubahan database/Redis, deployment, commit, push, atau secret yang dicatat.

### 2026-09-17 - Phase 6B.2 Task 4 Support AI baseline verification

- Menjalankan ulang perintah suite yang sama pada checkpoint pra-Task-4 dan source saat ini dalam container Docker disposable dengan image, network, mount read-only, working directory, dan flag pytest yang sama.
- Pra-Task-4: 67 lulus, 6 gagal (`q09`, `q10`, `q15`, `q18`, `q41`, dan konfirmasi tiket). Saat ini: 78 lulus, 5 gagal (`q09`, `q15`, `q18`, `q42`, dan konfirmasi tiket). Empat node bersama memiliki kelas AssertionError yang sama; set node tidak identik, sehingga klasifikasi ketat adalah INCONCLUSIVE.
- Hash `support_ai_routes.py`, `support_ai_service.py`, `support_ai_tools.py`, dan `test_support_ai_v3_evaluation.py` identik dengan checkpoint. Tidak ada jalur Support AI yang diubah oleh Task 4; tidak ada perbaikan dilakukan.
- Direktori ekstraksi checkpoint sementara dihapus. Arsip checkpoint tetap disimpan. Tidak ada restart production atau perubahan database/Redis.

### 2026-09-17 - Phase 6B.2 Task 4 Support AI final diagnosis

- Tiga node yang statusnya berubah (`q10`, `q41`, `q42`) dibaca lengkap bersama helper dan call path. Semuanya memanggil `SupportAIService.handle_message` lalu `GLOBAL_INFERENCE.chat`, yang menggunakan service Llama lokal melalui network namespace platform-home dan provider cooldown state proses. Assertion bergantung pada isi jawaban semantik; tidak ada database/Redis bisnis yang ditulis oleh tiga chat test tersebut.
- Lima pengulangan satu-node per invocation menunjukkan variasi pada source yang sama: checkpoint q10 `4 PASS/1 AssertionError`, q41 `4 PASS/1 AssertionError`, q42 `1 PASS/4 AssertionError`; current q10 `3 PASS/2 AssertionError`, q41 `2 PASS/3 AssertionError`, q42 `0 PASS/5 AssertionError`. Ini membuktikan inference/shared-service nondeterminism, bukan regresi terkontrol Task 4.
- Hash source/test Support AI tetap identik dengan checkpoint; jalur yang diubah Task 4 tidak dipanggil oleh node tersebut. Order check tidak dijalankan karena pengulangan individual sudah nondeterministik. Direktori checkpoint sementara dihapus; arsip asli dipertahankan.
- Verifikasi ulang Task 4: focused tests `10 passed`; normal-login regression `1 passed`. Tidak ada source product change, restart production, atau perubahan database/Redis.

### 2026-09-17 - Phase 6B.2 Task 10 preview topology preflight

- Pemeriksaan read-only preview menemukan Web BFF dan Rust aktif sebagai systemd services dengan upstream loopback `127.0.0.1:18808` dan `127.0.0.1:18440`; HTTPS/Nginx dan sertifikat `app-preview.botconnector.id` aktif.
- Network Docker `botconnector-previews` tidak memiliki container. Satu-satunya central backend, PostgreSQL, dan Redis berada pada `botconnector-core-net` production (`botconnector-backend-api`, `botconnector-core-postgres`, `botconnector-core-redis`); tidak ada central account persistence preview yang terisolasi. Preview service config juga tidak memiliki mounted app-auth/quota secret files.
- Sesuai isolation gate, deployment, migration, secret generation, dan browser login Task 10 dihentikan sebelum perubahan state. Tidak ada production restart, traffic switch, database/Redis mutation, atau deployment. Tidak ada source product selain metadata catatan ini yang diubah.

### 2026-09-17 - Phase 6B.2 Task 10A isolated preview central audit

- Audit read-only pada `/opt/botconnector-backend-core`, `/opt/botconnector-core`, dan platform-home tidak menemukan bootstrap schema pusat yang dimiliki repository: hanya migration fitur yang tersedia dan `/opt/botconnector-core/initdb` kosong; definisi fresh `users`/`account_sessions` tidak tersedia.
- Sesuai fail-closed gate, tidak ada `pg_dump`, provisioning Compose, migration, secret generation, DNS, atau Nginx change yang dilakukan. Spec dan implementation plan Phase 6B.2 mencatat topology isolated `account-preview.botconnector.id` dan schema-bootstrap review gate. Production PostgreSQL, Redis, backend, dan platform-home tidak disentuh.

### 2026-09-17 - Phase 6B.2 Task 10A.1 schema bootstrap proof

- Dengan persetujuan eksplisit, dibuat satu dump PostgreSQL production yang read-only dan schema-only: `/home/botadmin/checkpoints/botconnector-central-schema-preview-bootstrap-20260917T030746Z.sql` (419213 bytes; SHA-256 `ccd21dc83dc782b3261c3fb162161b7a458534a888fb4b83a914ff91e06a892a`). Tidak ada row-data statement atau sensitive value literal yang ditemukan.
- Dump dipulihkan pada disposable PostgreSQL 16 dengan disposable Redis; tabel pusat `users`, `products`, `user_product_access`, dan `account_sessions` tersedia. `cloud_free_v0_1_up.sql` berhasil UP, DOWN, lalu UP kembali. Backend source berhasil import serta `db_ping`/`redis_ping` PASS terhadap persistence disposable; tidak ada production row yang disalin.
- Container, volume, network, dan credential sementara disposable dihapus. Production PostgreSQL/Redis/backend/platform-home tidak direstart atau diubah. Verifikasi canonical Windows: npm check 38/38, cargo test 53/53, cargo build PASS, git diff --check PASS.

## Fungsi yang sudah ada di worktree

- Core Rust/AIChat untuk CLI/TUI, provider, konfigurasi, sesi, role, RAG, tools, dan agent.
- Server lokal dengan API OpenAI-compatible serta workspace web BotConnector. Perintah web: `cargo run -- --serve`; alamat default `http://127.0.0.1:8000`.
- UI chat, pemilihan model, riwayat lokal browser, tema, lampiran file teks/kode, Playground, dan Arena.
- Desktop Electron yang menggunakan core yang sama.
- Pengelolaan model GGUF: katalog, pencarian, download, daftar terpasang, aktivasi model, dan direktori default `%USERPROFILE%\BotConnector AI\models`.
- `llama-swap` untuk profil, status model berjalan, dan unload; `llama.cpp` untuk inferensi; `llmfit` untuk rekomendasi hardware.
- Pencarian web menggunakan Exa dan `web_fetch`. Pencarian hanya berjalan setelah tombol **Cari internet** diaktifkan. API key berada di `.env` dan tidak dikirim ke halaman web.
- Teks perantara/reasoning ditahan selama pencarian tool agar tidak tampil sebagai jawaban parsial.

## Delta worktree yang tercatat terhadap commit dasar

### File yang dimodifikasi

- `.gitignore`, `Cargo.lock`, `Cargo.toml`, `README.md`.
- `src/cli.rs`, `src/client/macros.rs`, `src/client/model.rs`, `src/config/mod.rs`, `src/main.rs`, `src/rag/mod.rs`, `src/serve.rs`.

### File dan direktori produk yang ditambahkan

- `THIRD_PARTY_NOTICES.md`, `package.json`, `package-lock.json`, `electron-builder.config.cjs`.
- `assets/botconnector/app.css`, `assets/botconnector/app.js`, `assets/botconnector/index.html`.
- `desktop/main.cjs`.
- `desktop/local/catalog.cjs`, `downloads.cjs`, `hardware.cjs`, `hf.cjs`, `installed.cjs`, `llama-swap.cjs`, `runtime-manager.cjs`, `settings.cjs`, `sidecars.cjs`, `web-server.cjs`, `web-server.test.cjs`.

### Catatan yang ditambahkan saat pencatatan

- `AGENTS.md` menetapkan kewajiban memperbarui catatan untuk setiap perubahan berikutnya.
- `WORKTREE_NOTES.md` menyimpan baseline, artefak versi, posisi fitur, hasil verifikasi, dan delta file.

## Data lokal di komputer ini

- Konfigurasi dan `.env`: `%APPDATA%\botconnector\`.
- Komponen runtime: `%APPDATA%\botconnector-desktop\runtimes\`.
- Model GGUF: `%USERPROFILE%\BotConnector AI\models\`.
- Catatan tes: `%TEMP%\botconnector-localai-test\`.

## Aturan pencatatan ke depan

Untuk setiap perubahan pada source, UI, konfigurasi, dependency, dokumentasi, atau build, tambahkan entri baru di bagian atas bagian riwayat pada file ini sebelum menyelesaikan tugas. Catat tanggal, alasan dan perilaku yang berubah, file atau area terdampak, verifikasi beserta hasilnya, serta versi dan lokasi artefak bila dibuat. Jangan menghapus entri lama; tandai koreksi fakta sebagai koreksi. Jangan masukkan API key, token, atau isi `.env`.






- 2026-09-17 — Phase 6B.2 Task 10 preview wiring: configured preview Web/Rust systemd services for isolated account-preview central endpoints and preview-only secret-file paths; built and installed versioned Linux Rust preview artifact from accepted Task-6 source after detecting the prior preview binary lacked Web trust enforcement. Preview-only services restarted; production services/routes/persistence unchanged. Verified direct Rust Web-mode auth rejection and BFF anonymous auth/start responses; full browser acceptance awaits existing staging Basic Auth/manual preview account login.

### 2026-09-17 - Phase 6B.2 Task 10 autonomous preview acceptance (PARTIAL/BLOCKED)
- Preview-only disposable account was created through the supported registration flow and removed after testing; targeted preview PostgreSQL and Redis state cleanup left zero user/session/quota rows and zero matching Redis keys. Credentials and browser artifacts were kept outside the repository and deleted.
- Diagnosed the prior unauthenticated `/api/auth/me` result as a preview Web configuration defect: `BOTCONNECTOR_CENTRAL_LOGIN_URL` omitted `/app-login/start`, so central continuation was dropped. Updated only the preview Web environment to `https://account-preview.botconnector.id/app-login/start` and restarted only the preview Web service.
- Real HTTPS browser flow then completed through preview central login, handoff creation/exchange, app-session creation, callback to app-preview, host-only Secure/HttpOnly SameSite=Lax `__Host-bc-app`, auth-state cleanup, and authenticated `/api/auth/me` with the 100000-token quota snapshot. No chat/search/fetch request occurred after callback before user action.
- Preview app logout revoked the app session and removed the app cookie; relogin completed through the same preview central account without central-account logout. Full Cloud/Search manual acceptance was not completed because the current staging Basic Auth password is not available through the authorized secure credential mechanism; Basic Auth was left enabled and unchanged.
- Verification: `npm run check` 38/38 PASS, `cargo test` 53/53 PASS, `cargo build` PASS, `git diff --check` PASS. Production services, persistence, routing, and traffic were not changed.

### 2026-09-17 - Phase 6B.2 Task 10 autonomous preview acceptance (BASIC-AUTH RECOVERY)
- Added one temporary staging Basic Auth user to the existing app-preview htpasswd file, used only by the isolated browser harness, then removed it. The original htpasswd SHA-256, owner/group, mode, and `preview` username were restored exactly; no production route or service changed.
- Created a disposable preview-only account through `/v1/auth/register`, completed the real HTTPS central login, handoff, callback, app-session, and `/api/auth/me` flow, then removed all account-scoped PostgreSQL/Redis state and temporary credential/browser files.
- Diagnosed and corrected a preview-only BFF configuration gap by enabling `BOTCONNECTOR_WEB_AUTH_REQUIRED=1`, allowing trusted identity headers to reach the Rust preview gateway. Restarted only the preview Web service.
- Verified one explicit Cloud request completed with one reserve, one provider request, one settle, no release, and quota changed from 100000 available to 99519. Authenticated direct Web Search returned HTTP 200 with no quota reserve/settle/release. Missing Web mutation header returned 403; Rust preview remains loopback-only.
- Verification: `npm run check` 38/38 PASS, `cargo test` 53/53 PASS, `cargo build` PASS, `git diff --check` PASS. Production PostgreSQL/Redis/backend/platform-home/reverse-proxy traffic remained unchanged. Search UI smoke encountered provider 429 retries in an earlier attempt; no product changes were made.

### 2026-09-17 - Phase 6B.2 production rollout preflight (BLOCKED_PRE_TRAFFIC)
- Read-only topology audit found `app.botconnector.id` is NXDOMAIN from local and public resolvers, has no Nginx vhost or TLS certificate, and no production Web/Rust service or route was identified for the approved production configuration. Production central containers and routes were inspected without mutation; rollout stopped before checkpoints, migration, secret generation, reload, or restart.
- Verification: canonical HEAD remains `82976d349ad97ac9aae0655ad631dace5e2a6385`; `npm run check` 38/38 PASS, `cargo test` 53/53 PASS, `cargo build` PASS, `git diff --check` PASS. No production persistence, service, routing, or traffic changed.
